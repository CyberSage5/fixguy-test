name: Run FixGuy

on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  fixguy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: CyberSage5/fixguy@v1.0.6  # Ensure it’s v1.0.6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
